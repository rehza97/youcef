# ============================================================================
# CORE DEPENDENCIES - Minimal set to get server running
# Install these first if full requirements.txt fails
# ============================================================================

# Core Framework (REQUIRED)
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database (REQUIRED)
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# Security (REQUIRED)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==41.0.7

# Configuration (REQUIRED)
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
email-validator==2.1.0      # Required by pydantic for email fields

# Basic Data Processing (REQUIRED)
pandas==2.1.4
openpyxl==3.1.2
numpy==1.26.4               # Python 3.12 compatible

# WebSockets (REQUIRED)
websockets==12.0

